package com.zhongjia.subtitlefusion.model.enums;

/**
 * 统一的叠加动效类型（图片、SVG、drawText 共用 ）。
 */
public enum OverlayEffectType {
    FLOAT_WAVE,
    LEFT_IN_RIGHT_OUT,
    TOP_IN_FADE_OUT,
    LEFT_IN_BLINDS_OUT,
    BLINDS_IN_CLOCK_OUT,
    FADE_IN_FADE_OUT
}
