# ========== 应用 ==========
# 对外端口（映射容器 8081）
APP_PORT=8081
# JVM 参数（如需开启鉴权，在下一行示例里追加 -D 开关）
JAVA_OPTS="-Xms512m -Xmx1024m"
# 示例：启用鉴权（去掉下一行注释）
# JAVA_OPTS="-Xms512m -Xmx1024m -Dsubtitlefusion.auth.enabled=true -Dsubtitlefusion.auth.tokens=sf_token_2024_admin"

# 任务存储：memory 或 redis
TASK_STORAGE_TYPE=memory

# ========== Redis（同机 Compose） ==========
REDIS_HOST=redis
REDIS_PORT=6379

# ========== MinIO（同机 Compose） ==========
# 对外映射端口（与 docker-compose.yml 保持一致）
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# 应用容器内访问 MinIO（Compose 内部网络）
MINIO_ENDPOINT=http://minio:9000

# 客户端/浏览器访问的外部地址（本机或公网）
# 若对外服务，改为 http://你的公网IP:9000 或域名
MINIO_EXT_ENDPOINT=http://127.0.0.1:9000

# MinIO 凭据（建议生产修改）
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

# 自动创建的桶名
MINIO_BUCKET=nis

# ========== 字幕渲染样式（通过环境变量覆盖） ==========
# 字体：族与样式（plain|bold|italic|bolditalic）
# 含空格的值必须加引号
SUBTITLE_FONT_FAMILY="Microsoft YaHei"
SUBTITLE_FONT_STYLE=plain
# 字号：二选一
# 方式A：直接像素
# SUBTITLE_FONT_SIZE_PX=32
# 方式B：相对比例（基于默认 h*0.035）
# SUBTITLE_FONT_SCALE=1.0
# 最小字号像素（避免自适应过小）
SUBTITLE_MIN_FONT_SIZE_PX=14
# 阴影/描边：不透明度(0-255) 与 半径（像素偏移范围）
SUBTITLE_SHADOW_ALPHA=180
SUBTITLE_SHADOW_RADIUS_PX=2
# 颜色：可用 #RRGGBB 或 R,G,B
# 字体颜色（示例：黄色）
SUBTITLE_FONT_COLOR=#FFFF00
# 阴影/描边颜色（默认黑色）
SUBTITLE_SHADOW_COLOR=#000000