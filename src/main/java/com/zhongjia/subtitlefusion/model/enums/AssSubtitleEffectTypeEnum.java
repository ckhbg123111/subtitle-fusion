package com.zhongjia.subtitlefusion.model.enums;

public enum AssSubtitleEffectTypeEnum {
    /** 键盘打字 + 光标闪烁 */
    TYPEWRITER_CURSOR,
    /** 左侧滑入，末端轻微回弹 */
    LEFT_IN_BOUNCE,
    /** 关键字高亮（加粗、放大、变色） */
    KEYWORD_HIGHLIGHT,
    /** 默认（无特殊动效，仅样式） */
    DEFAULT
}
